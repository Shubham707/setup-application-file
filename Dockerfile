//////////////Docker tool ////////////////

FROM OPENJDK:18
WORKDIR /usr/src/bootapp
copy . /usr/src/bootapp

CMD ["java","-jar",Blogapp-file-.jar"]






















command-----------------
site //docker hub 

docker pull python
docker pull openjdk:18
docker search mysql
docker run python (docker run --name python)
docker run openjdk
docker run detach (-d)
docker run --name containename -d 3asdsdssd  ( container id)

docker exec -it id python3
docker inspect  container id   /show all
docker -v

docker ps

docker ps a

docker images

docker rmi 1

docker build -t boottest .

docker run --name bootapptest -it boottest


docker ps -a

docker rm //any id

docker-compose up

docker-compose up -d

docker-compose down

docker run --name javacontainer -it -d openjdk

docker exec -it javas]container jshell (program)

docker pull --name mysqlDb -e MYSQL_root_PASSWORD=root -d mysql

docker exec -it mysqlDb bash
docker run --name nginxServer -d -p 8080:8080 nginx

docker stop (stop not remove)
docker rmi (image name)

docker build -t myubuntuImage .








wsl install
winver
task manager